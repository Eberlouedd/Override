Le code source attend un login est un serial.

dans la fonction auth() qui attend le login et un int, la fonction fais des calcule a partir de la chaine de
caractere de login et la stock dans une variable v4. ensuite la fonction auth() retourne 0 si le second parametre fourni
est egal a v4.

Si auth renvoie 0 dans main() alors un shell est execute

nous allons faire un programme en C qui va repoduire les calacule que l'ont fait sur la chaine de caractere de login pour
obtenir v4, et nous allons l'afficher.

Nous n'avons qu'a relancer level06 avec un login et ce que nous avons obtenu avec le script dans serial

cat /home/users/level07/.pass